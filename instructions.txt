To clone a repository: git clone <url>

Things to do:
1. To update: git fetch origin master
2. To add files to staging area: git add *
3. To commit files: git commit -m "<message>"
4. To push files to remote git repository: git push origin master
